# Minimal CI for DPLifting (GitLab)
stages:
  - build

build_and_test:
  stage: build
  image: gcc:11
  script:
    - make clean
    - make
    - make test
    - make example
    - ./example
  artifacts:
    paths:
      - libdplifting.so
    expire_in: 1 week
